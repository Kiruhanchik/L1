let {dates} = require('./task16'); // в этом файле проверяем работу. Импортируем функцию и вызываем ее.
                                    // выводит дату без прямого подключения либы moment.
dates();